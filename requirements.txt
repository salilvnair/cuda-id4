# NOTE: PyTorch is installed separately in setup_and_run.sh with the correct CUDA wheel.
# NOTE: diffusers is installed from git main in setup_and_run.sh (Ideogram4Transformer2DModel
#       is not yet in a released pip version). The line below is a floor only.
diffusers>=0.33.0
transformers>=4.45.0
accelerate>=1.0.0
huggingface-hub>=0.26.0
torchao>=0.5.0
sentencepiece
protobuf
Pillow>=10.0.0
python-dotenv>=1.0.0
openai>=1.0.0
anthropic>=0.25.0
requests>=2.31.0
